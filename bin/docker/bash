#!/bin/bash
####################################################
# Open bash in Local Docker Environment
#
# Usage:
# bin/docker/bash [OPTIONS]
# Options
# Any option supported by bash, is passed through

set -e

docker-compose run web bash $@
