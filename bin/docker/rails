#!/bin/bash
####################################################
# Run Rails Command in Local Docker Environment
#
# Usage:
# bin/docker/rspec [OPTIONS]
# Options
# Any option supported by rspec, is passed through

set -e

docker-compose run web bundle exec rails $@
